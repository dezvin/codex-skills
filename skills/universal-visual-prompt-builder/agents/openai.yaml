interface:
  display_name: "Universal Image Prompt Builder"
  short_description: "Build and repair portable image prompts"
  default_prompt: "Use $universal-visual-prompt-builder to turn this visual brief into a production-ready image prompt."

policy:
  allow_implicit_invocation: true
