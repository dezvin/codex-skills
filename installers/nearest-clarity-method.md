<!-- nearest-clarity-reference-core:start -->
# Метод ближайшей ясности

Статус: рабочая версия v0.6

Дата: 2026-06-12

## 0. Назначение

Метод ближайшей ясности нужен для сложных задач, где нельзя честно составить точный план до конца, но и нельзя действовать наугад.

Метод помогает LLM-партнёру и человеку вести работу так, чтобы после каждой итерации становилось понятнее:

- какой целевой контур удерживаем;
- что уже достаточно ясно;
- что пока остаётся гипотезой;
- какой ближайший участок можно планировать;
- какой сигнал дала последняя итерация;
- нужно ли обновить карту решений.

Метод применим к разным задачам: запуск продукта, контент, курс, система, исследование, личное планирование, пересборка проекта, сложное решение.

Метод не заменяет предметную экспертизу. Он задаёт способ работы с неопределённостью, зависимостями, гипотезами и новыми сигналами.

## 1. Короткая формула

```text
Держим целевой контур.
Намечаем карту решений.
Планируем ближайший участок.
Делаем одну итерацию.
Заранее называем ожидаемый сигнал.
Считываем сигнал.
Обновляем модель ситуации.
```

Иначе:

```text
Не планировать туман как будто он уже ясен.
Но и не действовать вслепую.
```

## 2. Основная проблема

В сложной работе есть две частые ошибки.

Первая - слишком жёсткий план. Человек или LLM расписывает весь путь заранее, хотя часть вводных ещё неизвестна. Такой план создаёт ложную уверенность и часто ломается при первом столкновении с реальностью.

Вторая - движение только ближайшим действием. Это выглядит гибко, но без общей карты легко превращается в блуждание: каждый отдельный шаг разумный, а общий путь не складывается.

Метод ближайшей ясности соединяет три слоя:

- целевой контур - чтобы понимать, ради чего идёт работа;
- рабочий план ближайшего участка - чтобы не расплываться;
- короткие итерации - чтобы учитывать новые сигналы.

## 3. Когда использовать метод

Использовать метод, если:

- работа должна продолжаться короткими ограниченными итерациями;
- пользователь не понимает, что делать дальше среди нескольких
  жизнеспособных направлений;
- важные данные или ограничения появятся только по ходу выполнения;
- текущий план или основание работы могли устареть;
- нужно выбрать ближайший проверяемый участок, не выдавая дальний путь за
  известный;
- весь маршрут нельзя честно спланировать заранее.

Не использовать метод только потому, что задача сложная, стратегическая,
рискованная или многошаговая, если маршрут и критерий готовности уже ясны. Не
использовать его для обычной приоритизации по заданным критериям.

Если нужен одноразовый пересмотр рамки с рекомендацией или планом, который
завершается за один проход, использовать самостоятельный `zoom-out`, а не
`nearest-clarity`.

## 4. Режимы глубины

Метод не всегда нужно включать полностью.

### 4.1. Лёгкий режим

Использовать для небольших задач с умеренной неопределённостью.

Достаточно ответить:

```text
Цель.
Ограничение.
Ближайшее действие.
Ожидаемый результат.
```

### 4.2. Рабочий режим

Использовать для обычных сложных задач.

Нужно пройти:

```text
целевой контур -> карта решений -> ближайший участок -> итерация -> сигнал -> следующий шаг
```

### 4.3. Глубокий режим

Использовать для стратегических, рискованных или дорогих решений.

Добавить:

- явные допущения;
- карту ограничений;
- риски;
- развилки;
- критерии успеха;
- точку согласования перед исполнением.

Если задача длинная, дорогая или многоходовая, LLM-партнёр может предложить вспомогательные списки:

- журнал сигналов;
- список гипотез;
- карту решений со статусами.

Это не обязательные документы, а опциональные рабочие опоры. Их не нужно создавать, если задача удерживается без них.

## 5. Термины метода

### Целевой контур

Рамка, которая отвечает на вопрос:

```text
Ради чего ведётся работа и что нельзя потерять?
```

Целевой контур фиксирует смысл, желаемый результат, границы и грубые ошибки выбора.

### Карта решений

Предварительная карта возможного пути.

Она показывает:

- вероятные этапы;
- развилки;
- риски;
- зависимости;
- решения, которые пока рано принимать;
- места, где нужна проверка.

Карта решений не является обещанием маршрута. Она обновляется по новым сигналам.

### Ближайший участок

Часть работы, которую уже можно планировать достаточно конкретно.

Ближайший участок отвечает:

- что делаем в текущей фазе;
- в каком порядке;
- почему именно так;
- какой результат должен появиться;
- что нельзя делать раньше времени.

### Итерация

Один короткий проход работы: действие или небольшой набор действий, после которых должен появиться результат или сигнал.

Перед итерацией нужно назвать ожидаемый сигнал: что именно должно стать понятнее после действия.

### Сигнал

То, что стало понятнее после итерации.

Сигналом может быть:

- факт;
- реакция;
- ограничение;
- противоречие;
- новая гипотеза;
- подтверждение;
- отказ от старой версии;
- новая зависимость;
- риск.

Не каждый шум является сигналом. Один случай не должен автоматически становиться правилом.

### Zoom out

Одноразовый обзор с выводом, рекомендацией или планом является самостоятельным
`zoom-out`.

Внутри активного `nearest-clarity` Zoom out является зависимой операцией: он
пересобирает рамку, когда новое знание меняет основание работы, после чего
управление возвращается в итерационный процесс.

### Тестовая итерация

Маленькая проба на реальном материале, если анализ не даёт достаточной ясности.

Тестовая итерация дорогая и используется редко: только после обоснования и явного согласия человека.

## 6. Градиент точности

План не должен быть одинаково подробным на всём расстоянии.

```text
текущая итерация -> максимально конкретно
ближайший участок -> рабочий план
дальше -> карта решений
самый дальний горизонт -> целевой контур
```

Ошибка - требовать от дальнего горизонта точности текущей итерации.

Другая ошибка - вообще не смотреть дальше ближайшего действия.

Дальние элементы нужно маркировать честно:

- **ориентир** - общий целевой контур;
- **возможный этап** - скорее всего понадобится, но форма может измениться;
- **развилка** - есть несколько путей, решение позже;
- **риск** - место, где может обнаружиться проблема;
- **обзорная проверка** - нужно подняться уровнем выше перед следующим решением.

## 7. Рабочий алгоритм

### 7.1. Перед началом участка

1. Определить, что мы пытаемся сделать.
2. Задать целевой контур.
3. Назвать входные данные.
4. Назвать ограничения.
5. Наметить карту решений.
6. Выделить ближайший участок.
7. Определить результат ближайшего участка.
8. Выбрать первую итерацию.

### 7.2. Во время итерации

1. Сформулировать фокус.
2. Назвать основание: данные, документы, наблюдения, вводные.
3. Назвать ожидаемый сигнал: что должно стать понятнее после действия.
4. Выполнить действие.
5. Считать фактический сигнал.
6. Отделить факт от гипотезы.
7. Проверить, не появилось ли новое знание, требующее Zoom out.
8. Назначить следующий шаг.
9. Зафиксировать статус результата.

### 7.3. После итерации

Ответить:

```text
Что сделали?
Какой сигнал ожидали?
Что стало понятнее?
Что осталось неизвестным?
Какой сигнал получен?
Что меняется в карте решений?
Какой следующий шаг?
Какой статус результата?
```

Минимальные статусы знания:

- **факт** - подтверждённая входная информация;
- **наблюдение** - замеченное проявление, которое ещё не стало правилом;
- **гипотеза** - проверяемое предположение;
- **решение** - выбранный следующий шаг или рамка действия;
- **риск** - место возможной ошибки или потери;
- **шум** - информация, которая не меняет решение;
- **проверенный результат** - вывод, который выдержал проверку и может использоваться дальше.

## 8. Работа с гипотезами

Гипотеза - это проверяемое предположение, а не решение.

Формат:

```text
Мы предполагаем, что...
Потому что...
Это важно, потому что...
Проверить можно через...
Если гипотеза верна, следующий шаг...
Если неверна, меняем...
```

Неожиданный хороший результат тоже не становится правилом сразу.

Сначала он получает статус гипотезы:

```text
Это сработало в этом случае.
Пока не ясно, почему.
Нужно проверить, повторяется ли это и при каких условиях.
```

Запрещено подгонять объяснение под удобную версию.

Если причина не ясна, нужно назвать несколько возможных объяснений и уровень уверенности.

## 9. Новое знание и Zoom out

Если появилось новое знание, которое меняет основание работы, нельзя просто продолжать по старому плану.

Сначала нужен Zoom out.

При первоначальном запуске `nearest-clarity` зависимый `zoom-out` нужен только
если пользователь явно просит пересобрать рамку или текущее основание
подтверждённо устарело. Сам по себе запрос следующего шага не требует
`zoom-out`.

Короткий Zoom out:

1. Какой это тип задачи?
2. Какая цель выше уровнем?
3. В какой системе живёт задача?
4. Какие входные данные и ограничения изменились?
5. Что является фактом, а что предположением?
6. Какие зависимости изменились?
7. Какие риски и слепые пятна появились?
8. Как теперь выглядит критерий хорошего результата?
9. Что нужно обновить: целевой контур, карту решений, ближайший участок или текущую итерацию?

Формула:

```text
Новое знание -> Zoom out -> обновление карты решений -> новая итерация.
```

Если новое знание не меняет основание работы, его можно учесть в текущем цикле без полной пересборки.

Zoom out считается завершённым только если после него стало ясно, что именно меняется: цель, ограничения, зависимости, риски, критерий результата, карта решений, ближайший участок или текущая итерация.

Если после Zoom out ничего не изменилось и следующий шаг не стал яснее, это был не рабочий пересмотр, а лишний обзор.

## 9.1. Сигнал и шум

Сигнал - это новая информация, которая меняет понимание задачи, ограничения, гипотезы, риски, порядок действий или критерий результата.

Шум - это информация, которая выглядит заметно, но не меняет решение.

Сигналом может быть: повторяющаяся реакция, сильное противоречие, новое ограничение, ошибка в основании, подтверждение гипотезы, провал гипотезы, появление более сильного пути.

Шумом может быть: единичная случайная реакция, эмоциональный всплеск без связи с решением, красивая идея без применения, новый факт без влияния на текущий участок.

Правило:

```text
Не каждый новый факт требует пересборки карты.
Но каждый факт, который меняет основание решения, требует Zoom out.
```

Перед обновлением карты решений нужно спросить:

- что именно изменилось;
- на какой уровень это влияет;
- это повторяемый сигнал или одиночный шум;
- какое решение стало другим из-за этой информации.

## 10. Ограничения и точки согласования

Перед рискованным действием нужно назвать ограничения:

- что нельзя нарушить;
- какие ресурсы ограничены;
- какие решения необратимы;
- где нужна проверка;
- где нужно согласие человека.

Точка согласования нужна, если действие меняет:

- документ;
- план;
- деньги;
- публичную коммуникацию;
- направление проекта;
- обязательства;
- необратимое решение.

Схема:

```text
анализ -> предложение -> явное согласие -> исполнение
```

Для маленьких обратимых действий этот цикл не нужно раздувать.

Для рискованных переходов его нельзя пропускать.

## 10.1. Матрица переходов

Эта матрица помогает выбрать следующий режим работы.

| Ситуация | Что делать |
|---|---|
| Итерация дала понятный сигнал, карта решений не изменилась | Продолжать ближайший участок |
| Итерация дала сигнал, который меняет порядок действий | Обновить рабочий план ближайшего участка |
| Появилось новое знание, которое меняет цель, ограничения или основание задачи | Сделать Zoom out |
| Сигнал слабый, одиночный или не влияет на решение | Зафиксировать как наблюдение, не перестраивать карту |
| Ближайший участок дал нужный результат | Закрыть участок и выбрать следующий |
| Участок не даёт сигнала после нескольких итераций | Сузить задачу или сделать Zoom out |
| Действие меняет документ, план, деньги, публичную коммуникацию или направление проекта | Запросить точку согласования |
| Анализа недостаточно для выбора следующего шага, но маленькая проба дешевле большого ошибочного плана | Предложить тестовую итерацию с обоснованием и ждать согласия |

## 11. Старый материал и старые планы

Если старый элемент не работает целиком, его не нужно сразу выбрасывать.

Сначала проверить:

- что в нём умерло;
- что устарело;
- что всё ещё полезно;
- можно ли извлечь живую часть;
- можно ли использовать как мост;
- нужно ли отложить до другого этапа.

Формула:

```text
Сначала извлечь живое.
Потом удалить мёртвое.
```

Удалять стоит только после проверки, что в элементе нечего сохранить.

## 12. Тестовая итерация

Иногда проект нельзя понять только из анализа. Тогда может понадобиться маленькая тестовая итерация на реальном материале.

Этот режим можно неформально называть “разведкой боем”, но рабочий термин - тестовая итерация.

Тестовая итерация не является стандартным действием метода.

Она допускается только если:

- обычный анализ не даёт достаточной ясности;
- без тестовой итерации нельзя выбрать следующий шаг;
- цена маленькой пробы ниже цены большого ошибочного плана;
- LLM-партнёр явно предлагает этот режим;
- LLM-партнёр объясняет, почему без него нельзя обойтись;
- человек явно одобряет тестовую итерацию.

Формула:

```text
Тестовая итерация - только по обоснованному запросу и после явного согласия.
```

Если тестовая итерация дала полезный результат, его нельзя сразу превращать в стандарт.

Правильная цепочка:

```text
тестовая итерация -> наблюдение -> гипотеза -> проверка -> правило
```

Неправильная цепочка:

```text
тестовая итерация -> сразу новый стандарт
```

## 13. Защита от ложного прогресса

Ложный прогресс:

- есть большой список тем, но нет архитектуры;
- есть тексты, но нет пути читателя;
- есть план, но неизвестно, какие гипотезы он проверяет;
- есть красивый документ, но он не помогает принять решение;
- есть обсуждение, но следующий шаг не стал яснее;
- есть скорость, но нет проверки направления;
- есть итерации, но нет фиксации сигналов;
- есть Zoom out, но нет действия после него.

Проверка:

```text
После этой итерации стало легче решить, что делать дальше?
```

Если нет, это активность, а не продвижение.

Хорошая итерация не обязана дать приятный результат. Она обязана дать рабочую ясность: подтвердить гипотезу, ослабить гипотезу, показать риск, уточнить ограничение, закрыть развилку или честно показать, что данных недостаточно.

Главный критерий метода:

```text
Стало ли легче принять следующее правильное решение?
```

## 14. Частые поломки метода для LLM

LLM должна избегать этих ошибок:

1. **Слишком ранний финальный план**
   Делать вид, что весь путь уже понятен.

2. **План без статусов**
   Не различать факт, гипотезу, решение и проверенный результат.

3. **Итерации без сигнала**
   Выполнять действия без ожидаемого сигнала и без понимания, что должно стать яснее.

4. **Zoom out как ритуал**
   Делать обзор ради вида, без обновления рамки, карты решений или следующего шага.

5. **Тестовая итерация как побег от решения**
   Предлагать пробу там, где уже достаточно данных для следующего шага.

6. **Удаление старого материала без разбора**
   Выбрасывать старый план или текст, не извлекая живые части.

7. **Терминологический шум**
   Использовать метод как набор красивых слов, а не как рабочий алгоритм.

8. **Самовольное исполнение рискованных действий**
   Менять документы, планы, публичную коммуникацию или направление проекта без точки согласования.

## 15. Когда фиксировать результат

Фиксировать нужно не каждую мысль, а только то, что должно пережить текущий разговор.

Фиксировать стоит:

- принятое решение;
- рабочую рамку;
- сильную гипотезу;
- следующий шаг;
- ограничение;
- критерий;
- ошибку, которую нельзя повторить;
- структуру, которую будем использовать дальше.

Не фиксировать:

- сырой шум;
- случайные идеи без статуса;
- промежуточные эмоции;
- списки “можно бы когда-нибудь” без следующего действия.

## 16. Чеклист применения

Перед началом:

```text
1. Что мы пытаемся сделать?
2. Зачем это нужно?
3. Какой целевой контур?
4. Какие входные данные есть?
5. Какие ограничения есть?
6. Что видно на карте решений?
7. Какой ближайший участок?
8. Какая итерация даст больше ясности?
9. Какой сигнал мы ожидаем от первой итерации?
```

После итерации:

```text
1. Что сделали?
2. Какой сигнал ожидали?
3. Какой сигнал получили?
4. Что стало фактом?
5. Что осталось гипотезой?
6. Нужно ли делать Zoom out?
7. Что меняется в карте решений?
8. Какой следующий шаг?
9. Какой статус результата?
```

## 17. Критерий хорошего прохода

Проход считается хорошим, если после него есть:

- один конкретный результат;
- заранее названный ожидаемый сигнал;
- ясный сигнал;
- честный статус результата;
- обновлённая карта решений, если она изменилась;
- понятный следующий шаг;
- меньше тумана, чем до прохода.

Не обязательно получить финальный ответ.

Достаточно честно сузить неопределённость.

## 18. Критерий завершения ближайшего участка

Ближайший участок можно закрывать, если:

- получен результат, ради которого участок запускался;
- основные сигналы считаны;
- ключевые гипотезы получили статус;
- карта решений обновлена;
- понятно, какой следующий участок открывается;
- нет незакрытого рискованного решения без согласования.

Если эти условия не выполнены, участок не закрыт. Нужно продолжить итерации, сузить задачу или сделать Zoom out.

<!-- nearest-clarity-reference-core:end -->

## 19. Установка в проект

Метод можно установить в проект как постоянное правило работы для LLM-партнёра.

Установка создаёт два обязательных слоя:

- самодостаточный контракт маршрутизации в корневом `AGENTS.md`;
- проектный skill `.agents/skills/nearest-clarity/` с полным рабочим регламентом.

`AGENTS.md` отвечает, когда выбрать `nearest-clarity`, и сохраняет его как родительский процесс. Skill отвечает, как выполнять метод, включая внутренний переход к глобальному `zoom-out` и возврат к итерационной работе.

Установка не создаёт новую task-систему, журнал сигналов или произвольные рабочие файлы. Если проект использует `TODO.md`, метод интегрируется с ним по правилам этого документа.

### 19.1. Когда устанавливать метод

Устанавливать метод в проект, если в нём регулярно встречаются задачи, где:

- путь нельзя честно распланировать до конца;
- старые планы часто устаревают;
- работа идёт через гипотезы, проверки и новые сигналы;
- есть риск ложной уверенности от слишком подробного плана;
- есть риск хаотичной импровизации без общей рамки;
- важно отделять факт, гипотезу, наблюдение, риск, шум и решение;
- нужны короткие циклы вместо большого неподтверждённого плана.

Не устанавливать метод как обязательный режим для всех задач.

Для простых задач достаточно обычного запроса, короткого плана или task-системы проекта, если она есть.

Единичная потребность в пересмотре рамки, рекомендации или плане за один
проход не является основанием для установки метода: такую задачу решает
самостоятельный `zoom-out`.

### 19.2. Что установка меняет

Установка добавляет в проектный `AGENTS.md` короткий самодостаточный контракт маршрутизации.

После установки LLM-партнёр должен:

- включать метод, когда работа должна продвигаться ограниченными итерациями, потому что полный маршрут ещё нельзя честно спланировать;
- не включать метод только из-за сложности, стратегичности, риска или многошаговости, если маршрут и критерий готовности уже ясны;
- не включать метод для обычной приоритизации по заданным критериям;
- использовать самостоятельный `zoom-out`, если нужен одноразовый пересмотр
  рамки с рекомендацией или планом без последующей итерационной работы;
- загружать проектный skill `nearest-clarity` при включении метода;
- сохранять `nearest-clarity` родительским процессом на всём протяжении итерационной работы;
- не строить ложный финальный план, если дальний путь ещё неясен;
- держать целевой контур;
- намечать карту решений как предварительную, а не как обещанный маршрут;
- планировать ближайший участок конкретнее дальнего горизонта;
- перед итерацией называть ожидаемый сигнал;
- после итерации называть фактический сигнал и отделять факт, наблюдение, гипотезу, риск, шум и решение;
- при первоначальном запуске вызывать зависимый `zoom-out` только по явному
  запросу пересобрать рамку или при подтверждённо устаревшем основании;
- использовать глобальный skill `zoom-out` как внутреннюю зависимую операцию, если новое знание меняет основание работы, затем возвращаться в `nearest-clarity`;
- не превращать метод в ритуал, если задача простая.

### 19.3. Что установка не меняет

Установка метода не должна:

- создавать `signals.md`, `hypotheses.md`, `decisions.md`, `state.md`, `progress.md` или другие файлы по умолчанию;
- менять существующую task-систему проекта;
- заменять `TODO.md`, если он уже используется;
- требовать полного метода для каждой мелкой задачи;
- заставлять LLM писать длинные отчёты после каждой итерации;
- сохранять весь ход рассуждений;
- превращать проектный `AGENTS.md` в копию этого документа.

Установка должна создать только канонические файлы skill:

```text
.agents/skills/nearest-clarity/
├── SKILL.md
├── agents/
│   └── openai.yaml
└── references/
    └── method.md
```

Дополнительные рабочие файлы допустимы только если они реально нужны для длинной, рискованной или многоходовой работы либо если пользователь явно попросил их создать.

### 19.4. Что прочитать перед установкой

Перед установкой Codex должен прочитать:

1. Этот документ.
2. Корневой проектный `AGENTS.md`, если он есть.
3. Корневой `TODO.md`, если он есть и проект использует task-систему.
4. Глобальный `skill-creator` и его правила создания и проверки skill.
5. Глобальный `zoom-out`, потому что он является зависимой операцией для пересмотра основания внутри метода.
6. Другие проектные инструкции только если без них нельзя безопасно встроить метод.

Не читать весь проект по умолчанию.

### 19.5. Inspect-pass перед записью

Перед изменением файлов Codex должен определить:

- найден ли корневой `AGENTS.md`;
- есть ли в нём уже правила работы с неопределённостью, итерациями, планированием или Zoom out;
- есть ли в проекте task-система;
- доступен ли глобальный `skill-creator`;
- доступен ли глобальный `zoom-out`;
- существует ли `.agents/skills/nearest-clarity/`;
- является ли существующий `nearest-clarity` предыдущей установкой этого пакета или отдельным пользовательским skill;
- есть ли в проекте отдельные правила, которые могут конфликтовать с методом;
- куда безопаснее встроить новый раздел;
- нужно ли добавлять интеграцию с `TODO.md`.

Если в `AGENTS.md` уже есть близкий раздел, обновить его по смыслу, а не добавлять второй дублирующий раздел.

Если найден предыдущий `nearest-clarity`, обновить три канонических файла без создания второго skill. Если найден другой одноимённый skill с несовместимым назначением или пользовательскими расширениями, не перезаписывать его молча: показать конфликт и запросить решение.

Если существующие правила противоречат методу и нельзя безопасно выбрать одну версию, остановиться и спросить пользователя.

Если глобальный `skill-creator` или `zoom-out` недоступен, не заявлять полную установку. Разрешено подготовить план или частичный слой, но статус должен прямо указывать отсутствующую зависимость.

### 19.6. Каноническая политика для AGENTS.md

Ниже указан обязательный смысл проектного раздела. Codex должен встроить его в стиль существующего `AGENTS.md`, сократить повторы и не оставлять следов установки.

```text
## Nearest Clarity Work Mode

Use `nearest-clarity` when continued work must advance through bounded
iterations because the full route cannot yet be honestly planned: the user
does not know what to do next among several viable directions, important inputs
will emerge during execution, the current plan may be stale, or the nearest
testable segment must be chosen without pretending the whole path is known.

Do not use it merely because work is complex, strategic, risky, or multi-step,
for ordinary prioritization by clear criteria, or when the route and done
condition are already clear. Use standalone `zoom-out` for a one-time
reframing, recommendation, or plan that can be completed in one pass.

When this mode applies, load
`.agents/skills/nearest-clarity/SKILL.md` and keep it as the parent workflow.
During that iterative workflow, use `zoom-out` only as the skill's dependent
reframing operation and then return to `nearest-clarity`.
```

Этот раздел должен быть самодостаточным как маршрутизатор. Не оставлять в `AGENTS.md` ссылку на этот установочный документ, историю установки или рабочий алгоритм метода.

В итоговом `AGENTS.md` должно быть явно видно:

- условие включения метода;
- условие невключения, когда маршрут и критерий готовности уже ясны;
- обязательность project skill `nearest-clarity`;
- сохранение `nearest-clarity` как родительского процесса;
- запрет заменять его самостоятельным `zoom-out` во время активной итерационной работы.

### 19.7. Каноническая структура project skill

Устанавливать skill только в проект:

```text
<project-root>/.agents/skills/nearest-clarity/
├── SKILL.md
├── agents/
│   └── openai.yaml
└── references/
    └── method.md
```

Не устанавливать этот пакет в глобальный каталог skills. Глобальный `zoom-out` остаётся отдельной зависимой операцией и не копируется в проект.

### 19.8. Канонический `.agents/skills/nearest-clarity/SKILL.md`

Содержимое файла ниже является готовым артефактом. При установке копировать его точно, не пересочинять и не дополнять историей установки.

````markdown
---
name: nearest-clarity
description: >-
  Use when continued work must advance through bounded iterations because the
  full route cannot yet be honestly planned: the user does not know what to do
  next among several viable directions, important inputs will emerge during
  execution, the current plan may be stale, or the nearest testable segment
  must be chosen without pretending the whole path is known. Do not use merely
  because a task is complex, strategic, risky, or multi-step, for ordinary
  prioritization by clear criteria, or when the route and done condition are
  already clear. Use standalone `zoom-out` for a one-time reframing,
  recommendation, or plan that can be completed in one pass. During iterative
  work, keep `nearest-clarity` as the parent and use `zoom-out` only through
  its internal transition.
---

# Nearest Clarity

Work through uncertainty without pretending that the whole route is already known.

## Internal Zoom Out

Keep `nearest-clarity` as the owner of the iterative workflow.

Use standalone `zoom-out` when the requested result is a one-time overview,
recommendation, or plan and no continued iterative workflow is needed.

At initial activation, invoke the global `zoom-out` skill as a dependent
operation only when the user explicitly asks to reframe the problem or the
current basis is confirmed stale. A request for a next step alone does not
require `zoom-out`.

When new knowledge changes the goal, constraints, dependencies, risks, success
criteria, or problem framing:

1. Invoke the global `zoom-out` skill as a dependent operation.
2. Apply its result to the target contour, decision map, and nearest segment.
3. Continue the `nearest-clarity` workflow from the updated basis.

Do not replace the parent workflow with standalone `zoom-out`.

If `zoom-out` is unavailable, do not claim that a full Zoom out was completed.
Stop before continuing on a stale basis and report the missing dependency.

## Choose Depth

- Use light mode for moderate uncertainty: target, constraint, nearest action, expected result.
- Use working mode for ordinary complex work: target contour, decision map, nearest segment, iteration, signal, next step.
- Use deep mode for strategic, expensive, high-risk, or long-running work. Read `references/method.md` before proceeding.

Do not turn a simple task into a methodology ritual.

## Establish The Work

1. State the target contour: why the work matters, what should become true, and what must not be lost.
2. Identify current sources, inputs, constraints, and approval boundaries.
3. Sketch a preliminary decision map: likely stages, forks, dependencies, risks, checks, and decisions that are too early to make.
4. Mark distant stages as provisional. Do not present them as a committed route.
5. Select the nearest clear segment that can be planned honestly.
6. Define the result that should exist when the nearest segment is complete.

Use this precision gradient:

```text
current iteration -> concrete
nearest segment -> working plan
later stages -> preliminary decision map
far horizon -> target contour
```

## Run An Iteration

Before acting:

1. Name the focus.
2. Name the evidence or input being used.
3. Name the expected signal: what should become clearer after the action.
4. Confirm that the action is inside current permissions and approval boundaries.

After acting:

1. State what was done.
2. Identify the actual signal.
3. Separate fact, observation, hypothesis, decision, risk, noise, and verified result.
4. State what remains unknown.
5. Choose the next transition.
6. Record only durable state when persistence is needed.

Keep user-facing updates concise. Do not expose hidden reasoning or create a long methodology report unless the user requests one.

## Choose The Transition

- If the signal does not change a decision, constraint, risk, order, or success criterion, treat it as noise or a bounded observation and continue.
- If the signal changes only the order or contents of current work, update the nearest segment.
- If the signal changes the goal, constraints, dependencies, risks, success criteria, or problem framing, use the global `zoom-out` skill before continuing.
- If the basis did not change, continue the nearest segment without a ritual review or another `zoom-out`.
- After the dependent `zoom-out`, update the affected working elements and continue under `nearest-clarity`.
- If the nearest segment produced its intended result, close it and select the next segment.
- If several iterations produce no useful signal, narrow the task or use `zoom-out`.

A Zoom out is complete only when it changes or confirms a concrete element of the work and makes the next step clearer.

## Handle Hypotheses

Treat a hypothesis as a testable assumption, not a decision or fact.

Use:

```text
We assume...
Because...
This matters because...
We can test it through...
If true, next...
If false, change...
```

Do not turn one successful case into a general rule. Use:

```text
test iteration -> observation -> hypothesis -> verification -> rule
```

## Respect Risk And Consent

Use the project's existing Gates and checkpoints.

Before risky or consequential actions that change documents, plans, money, public communication, project direction, obligations, or irreversible state:

1. Show the proposed action.
2. Explain the relevant consequence or risk.
3. Ask for explicit user confirmation.
4. Wait for a new user reply before executing.

Do not add an extra checkpoint for a small reversible action already directly requested by the user.

Use a test iteration only when analysis is insufficient, a small real-world probe is cheaper than a large wrong plan, and the user explicitly approves it.

## Integrate With TODO

The method controls movement through uncertainty. The project task system stores only durable state.

Using `TODO.md` as an information source does not make `manage-project-tasks`
the parent workflow. When the task is to choose a direction under genuine
uncertainty, keep `nearest-clarity` as the parent. Invoke
`manage-project-tasks` only if the resulting durable task state must be
persisted or reconstructed.

When work must survive the current session:

- use `Goal` for the target contour;
- use `Current Action` for the current iteration;
- keep `Items` limited to current and remaining work;
- use `Progress` for at most 1-3 meaningful results or signals;
- add `Constraints`, `Risks`, `Open Questions`, or `Decisions` only when needed for continuation;
- use `Proof` for available evidence;
- use `Next` for the next useful action.

Do not turn `TODO.md` into a reasoning transcript, signal journal, hypothesis dump, detailed decision map, or distant plan.

Knowledge labels do not replace the task statuses defined by the project.

## Keep Working State Minimal

Do not create signal logs, hypothesis lists, decision maps, state files,
progress files, or other working documents by default.

Create a separate artifact only when the work genuinely requires durable
structure outside the current task state or the user explicitly requests it.

## Finish A Pass

A good pass produces:

- one concrete result;
- an expected signal;
- an actual signal;
- an honest knowledge status;
- an updated nearest segment or decision map when needed;
- a clear next step;
- less uncertainty than before.

Use the final check:

```text
Is it now easier to choose the next correct step?
```

For detailed definitions, transition logic, failure modes, checklists, and examples, read `references/method.md`.
````

### 19.9. Канонический `.agents/skills/nearest-clarity/agents/openai.yaml`

Содержимое файла ниже является готовым артефактом. Копировать его точно.

```yaml
interface:
  display_name: "Nearest Clarity"
  short_description: "Короткие циклы для сложной работы"
  default_prompt: "Use $nearest-clarity to find the nearest clear segment, expected signal, and next correct step."

policy:
  allow_implicit_invocation: true
```

### 19.10. Канонический `.agents/skills/nearest-clarity/references/method.md`

Полный reference уже находится в этом документе и не должен пересочиняться при установке.

Собрать `references/method.md` точным копированием:

1. Скопировать содержимое между маркерами `nearest-clarity-reference-core:start` и `nearest-clarity-reference-core:end`, не включая сами маркеры.
2. Добавить один пустой ряд.
3. Скопировать содержимое между маркерами `nearest-clarity-reference-integration:start` и `nearest-clarity-reference-integration:end`, не включая сами маркеры.
4. Сохранить UTF-8 и LF.

Не включать в reference раздел установки, историю установки, validation-pass или инструкции `skill-creator`.

### 19.11. Порядок установки project skill

Выполнить установку как один согласованный setup-pass.

1. Определить корень проекта.
2. Найти активный глобальный `skill-creator` и полностью прочитать его `SKILL.md`.
3. Найти глобальный `zoom-out` и подтвердить, что его `SKILL.md` доступен.
4. Прочитать корневой `AGENTS.md` и `TODO.md`, если они существуют.
5. Проверить существующий `.agents/skills/nearest-clarity/`.
6. Если skill отсутствует, запустить `skill-creator/scripts/init_skill.py`:

```text
python "<skill-creator-dir>/scripts/init_skill.py" nearest-clarity --path "<project-root>/.agents/skills" --resources references --interface display_name="Nearest Clarity" --interface short_description="Короткие циклы для сложной работы" --interface 'default_prompt=Use $nearest-clarity to find the nearest clear segment, expected signal, and next correct step.'
```

7. Если обнаружена предыдущая совместимая установка, не запускать повторную инициализацию; обновить канонические файлы на месте.
8. Если одноимённый skill имеет другое назначение или неизвестные пользовательские расширения, остановиться до перезаписи и запросить решение.
9. Записать точное содержимое канонического `SKILL.md`.
10. Собрать `references/method.md` только из отмеченных reference-блоков этого документа.
11. Записать точное содержимое канонического `agents/openai.yaml`.
12. Встроить или обновить один канонический раздел `Nearest Clarity Work Mode` в корневом `AGENTS.md`.
13. Не создавать README, INSTALLATION_GUIDE, QUICK_REFERENCE, CHANGELOG или другие файлы skill.
14. Запустить:

```text
python "<skill-creator-dir>/scripts/quick_validate.py" "<project-root>/.agents/skills/nearest-clarity"
```

15. После успешной проверки сравнить три установленных файла с каноническим содержимым этого документа.

Все Markdown- и YAML-файлы записывать в UTF-8. После записи прочитать их обратно и проверить кириллицу.

### 19.12. Validation-pass после установки

После установки Codex должен выполнить один validation-pass:

1. Прочитать изменённый раздел `AGENTS.md`.
2. Проверить, что метод встроен как самодостаточный маршрутизатор, а не как ссылка на установочный документ или копия рабочего алгоритма.
3. Проверить, что в `AGENTS.md` явно указаны условия включения и невключения метода.
4. Проверить, что `AGENTS.md` требует project skill `nearest-clarity`.
5. Проверить, что `AGENTS.md` сохраняет `nearest-clarity` родительским процессом и не заменяет его самостоятельным `zoom-out`.
6. Проверить в `SKILL.md`, что изменение основания вызывает зависимую операцию `zoom-out`, её результат обновляет рабочие элементы, а управление возвращается в `nearest-clarity`.
7. Проверить в `SKILL.md` ближайший участок, ожидаемый сигнал, фактический сигнал, статусы знания, точку согласования, продолжение без ритуального обзора, минимальное рабочее состояние и критерий хорошего прохода.
8. Проверить, что полный текст метода не скопирован в `AGENTS.md`.
9. Проверить, что существует ровно один `.agents/skills/nearest-clarity/`.
10. Проверить, что skill содержит только `SKILL.md`, `agents/openai.yaml`, `references/method.md` и технические файлы, обязательные для самой среды, если она их создаёт.
11. Проверить, что `quick_validate.py` завершился успешно.
12. Сравнить `SKILL.md` и `openai.yaml` с каноническими блоками.
13. Сравнить `references/method.md` с точной сборкой двух reference-блоков.
14. Проверить, что `openai.yaml` содержит `$nearest-clarity` и `allow_implicit_invocation: true`.
15. Проверить, что глобальный `zoom-out` доступен и не был скопирован или изменён.
16. Проверить, что не созданы лишние файлы состояния, прогресса, сигналов или гипотез.
17. Проверить, что существующая task-система не перезаписана.
18. Проверить, что в `AGENTS.md` нет двух дублирующих правил работы с неопределённостью.
19. Статически проверить маршрутизацию:
    - настоящая неопределённость и продолжительная работа по новым сигналам
      включают `nearest-clarity`;
    - сложная задача с ясным маршрутом и обычная приоритизация по заданным
      критериям не включают его;
    - одноразовый пересмотр с рекомендацией или планом принадлежит
      самостоятельному `zoom-out`;
    - `TODO.md` как источник информации не включает
      `manage-project-tasks`, а сохранение durable task state передаётся ему
      только как отдельная операция.
20. Кратко сообщить пользователю, что изменено.

Формула статуса после установки:

`nearest-clarity AGENTS mode installed + project skill installed + skill validated + structurally validated + fresh-context routing not tested + not live-tested`

Статическая проверка формулировок не подтверждает автоматический выбор skill.
Не заявлять `fresh-context routing tested` без независимого запуска в свежем
контексте. Не заявлять `live-tested` без отдельной реальной задачи, где метод
прошёл полный цикл: целевой контур, ближайший участок, ожидаемый сигнал,
итерация, фактический сигнал, следующий шаг.

<!-- nearest-clarity-reference-integration:start -->
## 20. Интеграция с TODO.md

Метод ближайшей ясности может работать вместе с проектной task-системой, если в проекте установлен `TODO.md`.

Связь простая:

`Метод ближайшей ясности управляет движением. TODO.md фиксирует только то, что нужно не потерять.`

Метод не заменяет `TODO.md`.

`TODO.md` не должен становиться журналом сигналов, картой решений или стенограммой применения метода.

Использование `TODO.md` как источника информации не делает
`manage-project-tasks` владельцем процесса. Если нужно выбрать направление при
настоящей неопределённости, родителем остаётся `nearest-clarity`.
`manage-project-tasks` подключается только для отдельной операции сохранения
или восстановления долговременного task state.

### 20.1. Что попадает в TODO.md

Если задача должна пережить текущую сессию, её можно зафиксировать в `TODO.md`.

Для сложной задачи поля `TODO.md` используются так:

- `Goal` - целевой контур задачи: что должно стать правдой и что нельзя потерять.
- `Current Action` - текущая ближайшая итерация или рабочий шаг.
- `Items` - текущая и оставшаяся работа, а не вся история итераций.
- `Progress` - краткий итог уже полученного значимого сигнала или закрытого участка, максимум 1-3 пункта.
- `Constraints` - ограничения, которые реально влияют на следующий шаг.
- `Risks` - риски, которые могут изменить порядок действий или критерий результата.
- `Open Questions` - вопросы, без которых нельзя уверенно продолжить.
- `Decisions` - принятые решения, которые нужно помнить в будущей сессии.
- `Proof` - минимальное подтверждение текущего состояния или результата.
- `Next` - следующий полезный шаг.

### 20.2. Что не попадает в TODO.md

Не записывать в `TODO.md`:

- полный ход рассуждений;
- подробную карту решений;
- журнал всех сигналов;
- все гипотезы подряд;
- сырой Zoom out;
- промежуточный шум;
- дальний план, который ещё не может быть честно детализирован;
- эмоциональные реакции;
- случайные идеи без решения превратить их в задачу.

Если работа требует отдельной карты решений, списка гипотез, журнала сигналов или плана участка, Codex может предложить отдельный рабочий документ.

Создавать такой документ можно только если:

- без него трудно продолжать работу;
- задача длинная, рискованная или многоходовая;
- нужно передать состояние между сессиями;
- пользователь явно попросил зафиксировать такой слой.

### 20.3. Как метод влияет на статусы задач

Метод не добавляет новые task-статусы.

Если проектная task-система использует статусы `pending`, `in_progress`, `blocked`, `done`, `cancelled`, метод не меняет их.

Статусы знания метода - факт, наблюдение, гипотеза, решение, риск, шум, проверенный результат - не являются task-статусами.

Их можно кратко упоминать внутри `Progress`, `Risks`, `Open Questions` или `Decisions`, если это помогает продолжить задачу.

Пример:

`Signal: first test showed that the route works for one real case, but this is still an observation, not a rule.`

Не создавать в `TODO.md` отдельные статусы вроде:

- `hypothesis`;
- `validated`;
- `signal_received`;
- `zoom_out_needed`;
- `needs_clarity`.

Если нужна проверка, создать отдельную задачу проверки, а не новый статус.

### 20.4. Когда включать TODO.md при работе по методу

Сохранять результат через установленную task-систему, если:

- задача должна продолжиться в будущей сессии;
- появился следующий шаг, который нельзя потерять;
- есть блокер;
- нужна отдельная будущая проверка;
- принято решение, влияющее на дальнейшую работу;
- сложная задача разрослась и стала проектной задачей;
- пользователь явно попросил записать задачу.

Не создавать и не изменять task state, если:

- это короткая разовая итерация внутри текущего разговора;
- результат не должен пережить сессию;
- обсуждение пока не стало задачей;
- появилась сырая идея без цели и следующего действия;
- метод используется только как лёгкая рамка ответа.

Простое чтение `TODO.md` как входных данных выполняется напрямую и само по
себе не включает `manage-project-tasks`.

### 20.5. Closure и архив

Если задача, ведущаяся по методу, закрывается, закрытие происходит по правилам установленной task-системы.

Для `Codex_TODO_System` это означает:

- закрытая задача получает `done` или `cancelled`;
- результат, proof и follow-up уходят в `archive/tasks/YYYY-MM.md`;
- закрытая задача удаляется из `TODO.md`;
- если нужен следующий участок, создаётся новая `pending` задача.

В архив не нужно переносить весь методологический ход.

Архивная запись должна отвечать:

- что было закрыто;
- какой результат получен;
- чем это подтверждено;
- есть ли follow-up.

Если в проекте установлен `Codex_TODO_System`, использовать его правила closure-pass и archive-pass как источник истины для закрытия задач. Этот метод не должен вводить собственный порядок закрытия задач поверх task-системы.

### 20.6. Минимальная запись сложной задачи в TODO.md

Компактная запись сложной задачи должна содержать:

- название задачи;
- статус;
- цель;
- текущую итерацию или рабочий шаг;
- краткий прогресс, если он нужен;
- текущие и оставшиеся пункты;
- критерий готовности;
- минимальный proof;
- следующий шаг.

Не превращать такую запись в дневник применения метода.

Плохая запись выглядит так:

- записан весь ход рассуждений;
- перечислены все промежуточные сигналы;
- сохранён сырой Zoom out;
- перечислены все гипотезы без отбора;
- дальний горизонт расписан как точный план;
- нет ясного следующего действия.

### 20.7. Короткое правило интеграции

```text
Простая задача -> обычный ответ или короткий план.
Одноразовый пересмотр рамки -> самостоятельный zoom-out.
Продолжительная работа при настоящей неопределённости -> метод ближайшей ясности.
TODO.md как источник информации -> прямое чтение релевантных секций.
Сохранение долговременного состояния -> task-система через manage-project-tasks.
Закрытый результат -> архив задач.
Сырой ход мысли -> не фиксировать.
```

## 21. Короткая версия для вставки в работу

```text
Работаем по методу ближайшей ясности.

Держим целевой контур: зачем ведём работу и что нельзя потерять.
Намечаем карту решений: возможные этапы, развилки, зависимости и риски, не выдавая её за точный план.
Планируем ближайший участок: несколько зависимых шагов, которые уже можно пройти осмысленно.
Дальше идём короткими итерациями: фокус, основание, ожидаемый сигнал, действие, фактический сигнал, решение, статус.
Если новые данные меняют основание работы, сначала делаем Zoom out: уточняем цель, систему, факты, допущения, зависимости, риски и критерий результата. Потом обновляем карту решений.
Статус результата не завышаем: факт, наблюдение, гипотеза, решение, риск, шум и проверенный результат - разные вещи.
Тестовую итерацию используем редко: только если её необходимость обоснована и человек явно согласился.
Рискованные действия выполняем только через точку согласования: анализ, предложение, явное согласие, исполнение.
```
<!-- nearest-clarity-reference-integration:end -->
